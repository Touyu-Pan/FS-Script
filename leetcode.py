class Solution:
    def isValid(cls, s: str) -> bool:
        my_dict = {
            ")": "(",
            "]": "[",
            "}": "{",
        }
        idx = 0
        while idx < len(s):
            
       
    
res = Solution.isValid("()")
print(res)

